# DSI Studio AI Command Manual

The DSI Studio AI operating manual has been consolidated into
[`AGENTS.md`](AGENTS.md) so launcher, session, window, workflow, CLI/shell, demo, and
operational rules have one authoritative source.

Use `AGENTS.md` as the entry point. Topic-specific command inventories and workflow
documents remain linked from its **Related documents** section.

This compatibility file intentionally contains no duplicated operating instructions.
